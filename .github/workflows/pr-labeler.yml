name: labeler

on:
  pull_request:
    types: [opened]

jobs:
  triage:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/labeler@v3.1.0
      with:
        repo-token: "${{ secrets.GITHUB_TOKEN }}"
